import setuptools

setuptools.setup(
        name="twisted_ci",
        version="1.2",
        description="Compute twisted Hodge numbers of complete intersections",
        url="https://github.com/pbelmans/twisted-hodge-ci",
        packages=setuptools.find_packages(),
        zip_safe=False,
)
